# Ukrainian Children's Library — Frontend

React + TypeScript + Vite frontend for the Ukrainian children's lending library.

## Setup

```bash
npm install
npm run dev
```

Dev server runs on http://localhost:8080

## Environment Variables

Create a `.env` file:

```
VITE_API_URL=http://localhost:8000
```

## Tech Stack

- React 18 + TypeScript + Vite
- Tailwind CSS + shadcn/ui
- React Router, React Query, Zod
